{
  "name": "CodePass",
  "private": true,
  "version": "1.0.8",
  "type": "module",
  "description": "Gestionnaire de mots de passe",
  "homepage": "https://github.com/Damtis/gest_mdp",
  "repository": {
    "type": "git",
    "url": "https://github.com/Damtis/gest_mdp.git"
  },
  "author": "Jérémy Tarlié et Baptiste Damoiseau",
  "scripts": {
    "predev": "npm install && npm audit fix",
    "dev": "vite",
    "prepare-icons": "node scripts/prepare-icons.js",
    "build": "npm run prepare-icons && tsc && vite build && electron-builder --win --linux",
    "build:win": "npm run prepare-icons && tsc && vite build && electron-builder --win",
    "build:linux": "npm run prepare-icons && tsc && vite build && electron-builder --linux",
    "build:mac": "npm run prepare-icons && tsc && vite build && electron-builder --mac",
    "build:all": "npm run prepare-icons && tsc && vite build && electron-builder --win --linux --mac",
    "build:win:publish": "npm run prepare-icons && tsc && vite build && electron-builder --win --publish=always",
    "build:linux:publish": "npm run prepare-icons && tsc && vite build && electron-builder --linux --publish=always",
    "build:mac:publish": "npm run prepare-icons && tsc && vite build && electron-builder --mac --publish=always",
    "build:linux:docker": "docker run --rm -v ${PWD}:/project -w /project electronuserland/builder:wine npm run build:linux",
    "build:publish": "npm run prepare-icons && tsc && vite build && electron-builder --win --linux --publish=always",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "release": "node ../scripts/create-release.js",
    "check-permissions": "node ../scripts/check-permissions.js"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "electron-updater": "^6.6.2",
    "lucide-react": "^0.544.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.9.3",
    "react-toastify": "^11.0.5",
    "tailwindcss": "^4.1.13"
  },
  "devDependencies": {
    "@types/react": "^18.2.64",
    "@types/react-dom": "^18.2.21",
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "@vitejs/plugin-react": "^4.2.1",
    "dotenv": "^16.4.5",
    "electron": "^38.2.0",
    "electron-builder": "^24.13.3",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "picocolors": "^1.1.1",
    "typescript": "^5.2.2",
    "vite": "^7.1.7",
    "vite-plugin-electron": "^0.28.6",
    "vite-plugin-electron-renderer": "^0.14.5"
  },
  "main": "dist-electron/main.js",
  "overrides": {
    "tar": ">=7.5.8"
  }
}
